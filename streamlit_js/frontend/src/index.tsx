import React from "react"
import ReactDOM from "react-dom"
import StreamlitJS from "./StreamlitJS"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitJS />
  </React.StrictMode>,
  document.getElementById("root")
)
